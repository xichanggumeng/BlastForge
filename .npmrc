;# BlastForge Web 服务端部署配置：
;
; 生产环境默认跳过 Chromium 下载 —— 服务器通过 PUPPETEER_EXECUTABLE_PATH
; 指明系统已安装的 chromium / chrome / edge 路径。
;
; 启用 puppeteer 自动下载 Chromium（仅开发场景）：
;   npm config set puppeteer_skip_download=false && npm install
; 或本地：
;   PUPPETEER_SKIP_DOWNLOAD=false npm install
;
; 服务器在缺失 Chromium 时会被 PdfRenderError 明确指出，请运维准备浏览器。

puppeteer_skip_download=true
PUPPETEER_SKIP_DOWNLOAD=true
